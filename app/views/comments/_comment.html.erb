<% if comment.username %> <%# it shows one empty comment for some reason(( %>
<div class="alert alert-light">
    <p><strong><%= comment.username %></strong></p>
    <p><%= comment.body %></p>
    <p> 
        <%= link_to 'Delete', [comment.post, comment], method: :delete %>
    </p>
</div>
<% end %>